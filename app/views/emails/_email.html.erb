<%= bootstrap_form_for(@email ||= Email.new, layout: :inline) do |f| %>
  <%= f.text_field :address, hide_label: true, placeholder: "Enter Your Email!", icon: "user", class: "my-text-field" %>
  <%= f.submit "submit", class: "btn btn-md my-btn" %>
<% end -%>
